<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    
    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        primary: {
                            50: '#f5f3ff',
                            100: '#ede9fe',
                            200: '#ddd6fe',
                            300: '#c4b5fd',
                            400: '#a78bfa',
                            500: '#8b5cf6',
                            600: '#7c3aed',
                            700: '#6d28d9',
                            800: '#5b21b6',
                            900: '#4c1d95',
                        }
                    },
                    fontFamily: {
                        'inter': ['Inter', 'sans-serif']
                    }
                }
            }
        }
    </script>
    
    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    
    <!-- FontAwesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    
    <style>
        * {
            font-family: 'Inter', sans-serif;
        }
        
        body {
            background: linear-gradient(135deg, #f8fafc 0%, #f1f5f9 100%);
            min-height: 100vh;
        }
        
        .sidebar {
            background: linear-gradient(180deg, #1e293b 0%, #0f172a 100%);
            color: white;
        }
        
        .nav-link.active {
            background: rgba(124, 58, 237, 0.2);
            border-left: 4px solid #8b5cf6;
        }
        
        .nav-link:hover:not(.active) {
            background: rgba(255, 255, 255, 0.05);
        }
        
        .stats-card {
            background: white;
            border-radius: 16px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.05);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            border: 1px solid #e2e8f0;
        }
        
        .stats-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 30px rgba(0, 0, 0, 0.1);
        }
        
        .progress-bar {
            height: 8px;
            border-radius: 4px;
            overflow: hidden;
            background: #e2e8f0;
        }
        
        .progress-bar-fill {
            height: 100%;
            border-radius: 4px;
            transition: width 0.5s ease;
        }
        
        .api-key-container {
            background: linear-gradient(135deg, #8b5cf6 0%, #6366f1 100%);
            color: white;
        }
        
        .mobile-overlay {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: rgba(0, 0, 0, 0.5);
            z-index: 40;
            backdrop-filter: blur(4px);
        }
        
        .mobile-overlay.active {
            display: block;
        }
        
        @media (max-width: 768px) {
            .sidebar {
                position: fixed;
                left: -280px;
                top: 0;
                bottom: 0;
                width: 280px;
                z-index: 50;
                transition: left 0.3s ease;
            }
            
            .sidebar.active {
                left: 0;
            }
            
            .content {
                margin-left: 0;
            }
        }
        
        .code-block {
            background: #1a1a1a;
            color: #f8f8f2;
            border-radius: 8px;
            padding: 16px;
            overflow-x: auto;
            font-family: 'Monaco', 'Menlo', 'Ubuntu Mono', monospace;
            font-size: 14px;
            line-height: 1.5;
        }
        
        .table-container {
            border-radius: 12px;
            overflow: hidden;
            border: 1px solid #e2e8f0;
        }
        
        .table-container table {
            width: 100%;
            border-collapse: collapse;
        }
        
        .table-container th {
            background: #f8fafc;
            font-weight: 600;
            text-align: left;
            padding: 16px;
            border-bottom: 2px solid #e2e8f0;
        }
        
        .table-container td {
            padding: 16px;
            border-bottom: 1px solid #e2e8f0;
        }
        
        .table-container tr:hover {
            background: #f8fafc;
        }
        
        .plan-card {
            border-radius: 20px;
            transition: all 0.3s ease;
        }
        
        .plan-card:hover {
            transform: translateY(-8px);
        }
        
        .plan-card.popular {
            box-shadow: 0 20px 40px rgba(124, 58, 237, 0.15);
        }
    </style>
</head>
<body class="bg-gray-50">
    <!-- Mobile Overlay -->
    <div id="mobileOverlay" class="mobile-overlay" onclick="closeSidebar()"></div>
    
    <div class="flex min-h-screen">
        <!-- Sidebar -->
        <div id="sidebar" class="sidebar w-64 flex-shrink-0 hidden md:flex flex-col">
            <!-- Logo -->
            <div class="p-6 border-b border-gray-800">
                <div class="flex items-center">
                    <div class="w-10 h-10 rounded-xl bg-gradient-to-br from-purple-600 to-pink-600 flex items-center justify-center mr-3">
                        <i class="fas fa-comments text-white"></i>
                    </div>
                    <div>
                        <h1 class="text-xl font-bold text-white">WhatsGate</h1>
                        <p class="text-xs text-gray-400">API Dashboard</p>
                    </div>
                </div>
            </div>

            <!-- Navigation -->
            <nav class="flex-1 p-4 space-y-1">
                <a href="#overview" onclick="showSection('overview')" 
                   class="nav-link active flex items-center px-4 py-3 rounded-xl text-gray-200 hover:text-white transition-all duration-200">
                    <i class="fas fa-chart-line w-5 mr-3 text-center"></i>
                    <span class="font-medium">Overview</span>
                </a>
                
                <a href="#api" onclick="showSection('api')" 
                   class="nav-link flex items-center px-4 py-3 rounded-xl text-gray-200 hover:text-white transition-all duration-200">
                    <i class="fas fa-code w-5 mr-3 text-center"></i>
                    <span class="font-medium">API Docs</span>
                </a>
                
                <a href="#info" onclick="showSection('info')" 
                   class="nav-link flex items-center px-4 py-3 rounded-xl text-gray-200 hover:text-white transition-all duration-200">
                    <i class="fas fa-info-circle w-5 mr-3 text-center"></i>
                    <span class="font-medium">Information</span>
                </a>
                
                <a href="#upgrade" onclick="showSection('upgrade')" 
                   class="nav-link flex items-center px-4 py-3 rounded-xl text-gray-200 hover:text-white transition-all duration-200">
                    <i class="fas fa-rocket w-5 mr-3 text-center"></i>
                    <span class="font-medium">Upgrade</span>
                </a>
                
                <% if (user.role === 'admin') { %>
                <a href="/admin" 
                   class="flex items-center px-4 py-3 rounded-xl text-gray-200 hover:text-white transition-all duration-200">
                    <i class="fas fa-shield-alt w-5 mr-3 text-center"></i>
                    <span class="font-medium">Admin Panel</span>
                </a>
                <% } %>
            </nav>

            <!-- User Info -->
            <div class="p-4 border-t border-gray-800">
                <div class="flex items-center mb-4">
                    <div class="w-12 h-12 rounded-full bg-gradient-to-br from-purple-500 to-pink-500 flex items-center justify-center text-white font-bold text-lg">
                        <%= user.username.charAt(0).toUpperCase() %>
                    </div>
                    <div class="ml-3 flex-1">
                        <p class="font-semibold text-white"><%= user.username %></p>
                        <div class="flex items-center mt-1">
                            <span class="text-xs px-2 py-1 bg-gradient-to-r from-purple-600/20 to-pink-600/20 text-purple-300 rounded-full">
                                <%= user.plan.toUpperCase() %> PLAN
                            </span>
                        </div>
                    </div>
                </div>
                
                <button onclick="logout()" 
                        class="w-full flex items-center justify-center px-4 py-2.5 bg-gradient-to-r from-red-600 to-red-700 hover:from-red-700 hover:to-red-800 text-white rounded-xl font-medium transition-all duration-200 active:scale-95">
                    <i class="fas fa-sign-out-alt mr-2"></i>
                    Logout
                </button>
            </div>
        </div>

        <!-- Main Content -->
        <div class="flex-1 flex flex-col">
            <!-- Header -->
            <header class="bg-white shadow-sm border-b">
                <div class="px-6 py-4 flex items-center justify-between">
                    <div class="flex items-center space-x-4">
                        <button onclick="toggleSidebar()" 
                                class="md:hidden text-gray-700 hover:text-primary-700 p-2 hover:bg-gray-100 rounded-xl">
                            <i class="fas fa-bars text-xl"></i>
                        </button>
                        <div>
                            <h1 class="text-2xl font-bold text-gray-900">Dashboard</h1>
                            <p class="text-sm text-gray-600">Welcome back, <%= user.username %>!</p>
                        </div>
                    </div>
                    
                    <div class="flex items-center space-x-3">
                        <div class="hidden md:block px-4 py-2 bg-gradient-to-r from-primary-500 to-primary-600 text-white rounded-xl font-medium">
                            Limit: <%= user.requests_used_today %>/<%= user.daily_limit %>
                        </div>
                        <a href="https://t.me/yourtelegram" target="_blank" 
                           class="flex items-center px-4 py-2.5 bg-gradient-to-r from-blue-500 to-blue-600 hover:from-blue-600 hover:to-blue-700 text-white rounded-xl font-medium transition-all duration-200">
                            <i class="fab fa-telegram-plane mr-2"></i>
                            <span>Support</span>
                        </a>
                    </div>
                </div>
            </header>

            <!-- Content Area -->
            <main class="flex-1 p-6 overflow-y-auto">
                <!-- Overview Section -->
                <section id="overview-section" class="space-y-6">
                    <!-- Stats Grid -->
                    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
                        <!-- Card 1: Total Requests All Users -->
                        <div class="stats-card p-6">
                            <div class="flex items-center justify-between mb-4">
                                <div class="p-3 rounded-xl bg-gradient-to-br from-primary-50 to-primary-100">
                                    <i class="fas fa-globe text-primary-600 text-xl"></i>
                                </div>
                                <span class="text-sm font-medium text-gray-500">Global Total</span>
                            </div>
                            <p class="text-3xl font-bold text-gray-900" id="totalRequestsAll">
                                <%= totalRequestsAllUsers.toLocaleString() %>
                            </p>
                            <p class="text-sm text-gray-500 mt-1">All requests across all users</p>
                        </div>

                        <!-- Card 2: Your Total Requests -->
                        <div class="stats-card p-6">
                            <div class="flex items-center justify-between mb-4">
                                <div class="p-3 rounded-xl bg-gradient-to-br from-blue-50 to-blue-100">
                                    <i class="fas fa-chart-bar text-blue-600 text-xl"></i>
                                </div>
                                <span class="text-sm font-medium text-gray-500">Your Total</span>
                            </div>
                            <p class="text-3xl font-bold text-gray-900">
                                <%= user.total_requests.toLocaleString() %>
                            </p>
                            <p class="text-sm text-gray-500 mt-1">Your lifetime requests</p>
                        </div>

                        <!-- Card 3: Today's Usage -->
                        <div class="stats-card p-6">
                            <div class="flex items-center justify-between mb-4">
                                <div class="p-3 rounded-xl bg-gradient-to-br from-green-50 to-green-100">
                                    <i class="fas fa-calendar-day text-green-600 text-xl"></i>
                                </div>
                                <span class="text-sm font-medium text-gray-500">Today's Usage</span>
                            </div>
                            <p class="text-3xl font-bold text-gray-900">
                                <%= user.requests_used_today %>/<%= user.daily_limit %>
                            </p>
                            <div class="mt-4">
                                <div class="flex justify-between text-sm text-gray-600 mb-1">
                                    <span>Usage</span>
                                    <span><%= Math.round((user.requests_used_today / user.daily_limit) * 100) %>%</span>
                                </div>
                                <div class="progress-bar">
                                    <div class="progress-bar-fill bg-gradient-to-r from-green-500 to-green-600" 
                                         style="width: <%= (user.requests_used_today / user.daily_limit * 100) %>%"></div>
                                </div>
                            </div>
                        </div>

                        <!-- Card 4: Current Plan -->
                        <div class="stats-card p-6">
                            <div class="flex items-center justify-between mb-4">
                                <div class="p-3 rounded-xl bg-gradient-to-br from-yellow-50 to-yellow-100">
                                    <i class="fas fa-crown text-yellow-600 text-xl"></i>
                                </div>
                                <span class="text-sm font-medium text-gray-500">Current Plan</span>
                            </div>
                            <div class="flex items-center justify-between">
                                <p class="text-3xl font-bold text-gray-900 uppercase">
                                    <%= user.plan %>
                                </p>
                                <% if (user.plan !== 'free') { %>
                                <span class="px-3 py-1 bg-gradient-to-r from-green-500 to-green-600 text-white text-xs font-bold rounded-full">
                                    ACTIVE
                                </span>
                                <% } %>
                            </div>
                            <% if (user.plan_expires_at) { %>
                            <p class="text-sm text-gray-500 mt-2">
                                <i class="far fa-clock mr-1"></i>
                                Expires: <%= new Date(user.plan_expires_at).toLocaleDateString() %>
                            </p>
                            <% } %>
                        </div>
                    </div>

                    <!-- API Key Card -->
                    <div class="api-key-container rounded-2xl p-6">
                        <div class="flex items-center justify-between mb-6">
                            <div>
                                <h3 class="text-xl font-bold text-white mb-1">Your API Key</h3>
                                <p class="text-purple-200">Keep this secret and never share it publicly</p>
                            </div>
                            <div class="flex items-center space-x-2">
                                <button onclick="toggleApiKey()" 
                                        class="px-4 py-2.5 bg-white/10 hover:bg-white/20 text-white rounded-xl transition-all duration-200">
                                    <i id="eyeIcon" class="fas fa-eye mr-2"></i>
                                    <span id="eyeText">Show</span>
                                </button>
                                <button onclick="copyApiKey()" 
                                        class="px-4 py-2.5 bg-white hover:bg-gray-100 text-primary-700 rounded-xl font-medium transition-all duration-200 active:scale-95">
                                    <i class="fas fa-copy mr-2"></i>
                                    Copy
                                </button>
                            </div>
                        </div>
                        
                        <div class="relative">
                            <input type="password" 
                                   id="apiKeyInput" 
                                   value="<%= user.api_key %>" 
                                   readonly
                                   class="w-full px-4 py-3 bg-white/10 border border-white/20 rounded-xl text-white font-mono text-sm focus:outline-none focus:ring-2 focus:ring-white/50">
                            <div class="absolute right-3 top-1/2 transform -translate-y-1/2">
                                <span id="apiKeyStatus" class="text-xs text-green-300 hidden">
                                    <i class="fas fa-check mr-1"></i>Copied!
                                </span>
                            </div>
                        </div>
                        
                        <div class="mt-4 flex items-center text-purple-200 text-sm">
                            <i class="fas fa-shield-alt mr-2"></i>
                            <span>This key grants access to your account. Store it securely.</span>
                        </div>
                    </div>

                    <!-- Recent Activity -->
                    <div class="bg-white rounded-2xl shadow-sm border">
                        <div class="p-6 border-b">
                            <div class="flex items-center justify-between">
                                <h3 class="text-xl font-bold text-gray-900">Recent Activity</h3>
                                <span class="text-sm text-gray-500">
                                    Last <%= recentLogs.length %> requests
                                </span>
                            </div>
                        </div>
                        
                        <div class="table-container">
                            <table>
                                <thead>
                                    <tr>
                                        <th class="w-1/3">Endpoint</th>
                                        <th>Method</th>
                                        <th>Time</th>
                                        <th>Status</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <% if (recentLogs.length === 0) { %>
                                    <tr>
                                        <td colspan="4" class="text-center py-12">
                                            <div class="flex flex-col items-center justify-center">
                                                <div class="w-16 h-16 rounded-full bg-gray-100 flex items-center justify-center mb-4">
                                                    <i class="fas fa-inbox text-gray-400 text-2xl"></i>
                                                </div>
                                                <p class="text-gray-500 font-medium">No activity yet</p>
                                                <p class="text-gray-400 text-sm mt-1">Your API requests will appear here</p>
                                            </div>
                                        </td>
                                    </tr>
                                    <% } else { %>
                                        <% recentLogs.forEach(log => { %>
                                        <tr>
                                            <td class="font-mono text-sm">
                                                <%= log.endpoint %>
                                            </td>
                                            <td>
                                                <span class="px-3 py-1 rounded-full text-xs font-medium 
                                                      <%= log.method === 'GET' ? 'bg-blue-100 text-blue-800' : 
                                                         log.method === 'POST' ? 'bg-green-100 text-green-800' : 
                                                         'bg-gray-100 text-gray-800' %>">
                                                    <%= log.method %>
                                                </span>
                                            </td>
                                            <td class="text-gray-600">
                                                <%= new Date(log.created_at).toLocaleString() %>
                                            </td>
                                            <td>
                                                <span class="px-3 py-1 rounded-full text-xs font-medium bg-green-100 text-green-800">
                                                    <i class="fas fa-check mr-1"></i>Success
                                                </span>
                                            </td>
                                        </tr>
                                        <% }); %>
                                    <% } %>
                                </tbody>
                            </table>
                        </div>
                    </div>
                </section>

                <!-- API Documentation Section (Hidden by default) -->
                <section id="api-section" class="space-y-6 hidden">
                    <!-- API Docs content... (keep your existing API docs content, just update styling) -->
                </section>

                <!-- Information Section (Hidden by default) -->
                <section id="info-section" class="space-y-6 hidden">
                    <!-- Information content... -->
                </section>

                <!-- Upgrade Section (Hidden by default) -->
                <section id="upgrade-section" class="space-y-6 hidden">
                    <!-- Upgrade content... -->
                </section>
            </main>
        </div>
    </div>

    <!-- Toast Notification -->
    <div id="toast" class="fixed top-4 right-4 z-50 transform translate-x-full transition-transform duration-300">
        <div class="bg-gray-900 text-white px-6 py-4 rounded-xl shadow-xl flex items-center space-x-3 max-w-md">
            <i id="toastIcon" class="fas fa-check-circle text-green-400"></i>
            <div>
                <p id="toastMessage" class="font-medium">Copied to clipboard!</p>
            </div>
        </div>
    </div>

    <script>
        // Initialize Dashboard
        document.addEventListener('DOMContentLoaded', function() {
            console.log('Dashboard initialized');
            
            // Set active navigation based on URL hash
            const hash = window.location.hash.substring(1) || 'overview';
            showSection(hash);
            
            // Start stats updates
            startStatsUpdates();
            
            // Close sidebar on mobile when clicking outside
            document.addEventListener('click', function(event) {
                const sidebar = document.getElementById('sidebar');
                const mobileOverlay = document.getElementById('mobileOverlay');
                
                if (window.innerWidth < 768 && 
                    !sidebar.contains(event.target) && 
                    !event.target.closest('[onclick*="toggleSidebar"]') &&
                    sidebar.classList.contains('active')) {
                    closeSidebar();
                }
            });
        });

        // Sidebar Functions
        function toggleSidebar() {
            const sidebar = document.getElementById('sidebar');
            const overlay = document.getElementById('mobileOverlay');
            
            sidebar.classList.toggle('active');
            overlay.classList.toggle('active');
            
            // Prevent body scroll when sidebar is open
            if (sidebar.classList.contains('active')) {
                document.body.style.overflow = 'hidden';
            } else {
                document.body.style.overflow = '';
            }
        }

        function closeSidebar() {
            const sidebar = document.getElementById('sidebar');
            const overlay = document.getElementById('mobileOverlay');
            
            sidebar.classList.remove('active');
            overlay.classList.remove('active');
            document.body.style.overflow = '';
        }

        // Section Navigation
        function showSection(section) {
            // Hide all sections
            document.querySelectorAll('main > section').forEach(sec => {
                sec.classList.add('hidden');
            });
            
            // Show selected section
            const selectedSection = document.getElementById(section + '-section');
            if (selectedSection) {
                selectedSection.classList.remove('hidden');
            }
            
            // Update URL hash without scrolling
            history.replaceState(null, null, '#' + section);
            
            // Update active nav link
            document.querySelectorAll('.nav-link').forEach(link => {
                link.classList.remove('active');
                if (link.getAttribute('href') === '#' + section) {
                    link.classList.add('active');
                }
            });
            
            // Close sidebar on mobile
            if (window.innerWidth < 768) {
                closeSidebar();
            }
        }

        // API Key Functions
        function toggleApiKey() {
            const input = document.getElementById('apiKeyInput');
            const eyeIcon = document.getElementById('eyeIcon');
            const eyeText = document.getElementById('eyeText');
            
            if (input.type === 'password') {
                input.type = 'text';
                eyeIcon.className = 'fas fa-eye-slash mr-2';
                eyeText.textContent = 'Hide';
            } else {
                input.type = 'password';
                eyeIcon.className = 'fas fa-eye mr-2';
                eyeText.textContent = 'Show';
            }
        }

        async function copyApiKey() {
            const input = document.getElementById('apiKeyInput');
            const status = document.getElementById('apiKeyStatus');
            
            try {
                await navigator.clipboard.writeText(input.value);
                
                // Show success toast
                showToast('API key copied to clipboard!', 'success');
                
                // Temporarily show key
                const wasHidden = input.type === 'password';
                if (wasHidden) {
                    input.type = 'text';
                    setTimeout(() => {
                        input.type = 'password';
                    }, 2000);
                }
            } catch (err) {
                // Fallback for older browsers
                input.select();
                input.setSelectionRange(0, 99999);
                document.execCommand('copy');
                
                showToast('API key copied to clipboard!', 'success');
            }
        }

        // Toast Notification
        function showToast(message, type = 'success') {
            const toast = document.getElementById('toast');
            const toastMessage = document.getElementById('toastMessage');
            const toastIcon = document.getElementById('toastIcon');
            
            // Set message and icon based on type
            toastMessage.textContent = message;
            
            if (type === 'success') {
                toastIcon.className = 'fas fa-check-circle text-green-400';
            } else if (type === 'error') {
                toastIcon.className = 'fas fa-exclamation-circle text-red-400';
            } else if (type === 'warning') {
                toastIcon.className = 'fas fa-exclamation-triangle text-yellow-400';
            }
            
            // Show toast
            toast.classList.remove('translate-x-full');
            
            // Hide after 3 seconds
            setTimeout(() => {
                toast.classList.add('translate-x-full');
            }, 3000);
        }

        // Logout Function
        async function logout() {
            if (!confirm('Are you sure you want to logout?')) {
                return;
            }
            
            try {
                const response = await fetch('/auth/logout', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json'
                    }
                });
                
                if (response.ok) {
                    window.location.href = '/login';
                } else {
                    throw new Error('Logout failed');
                }
            } catch (error) {
                console.error('Logout error:', error);
                showToast('Logout failed. Please try again.', 'error');
            }
        }

        // Stats Updates
        let statsInterval;
        
        function startStatsUpdates() {
            // Update stats immediately
            updateStats();
            
            // Then update every 30 seconds
            statsInterval = setInterval(updateStats, 30000);
        }
        
        async function updateStats() {
            try {
                const response = await fetch('/api/stats');
                if (!response.ok) throw new Error('Failed to fetch stats');
                
                const data = await response.json();
                
                if (data.success) {
                    // Update total requests counter
                    const totalEl = document.getElementById('totalRequestsAll');
                    if (totalEl) {
                        totalEl.textContent = data.data.global.total_requests.toLocaleString();
                    }
                    
                    // You could update other stats here as needed
                }
            } catch (error) {
                console.warn('Stats update failed:', error);
            }
        }

        // Cleanup on page unload
        window.addEventListener('beforeunload', () => {
            if (statsInterval) {
                clearInterval(statsInterval);
            }
        });

        // Handle hash changes
        window.addEventListener('hashchange', function() {
            const hash = window.location.hash.substring(1) || 'overview';
            showSection(hash);
        });

        // Keyboard shortcuts
        document.addEventListener('keydown', function(event) {
            // Ctrl/Cmd + / to focus API key
            if ((event.ctrlKey || event.metaKey) && event.key === '/') {
                event.preventDefault();
                document.getElementById('apiKeyInput').focus();
                showToast('API key field focused', 'info');
            }
            
            // Escape to close sidebar
            if (event.key === 'Escape' && window.innerWidth < 768) {
                closeSidebar();
            }
        });
    </script>
</body>
</html>
