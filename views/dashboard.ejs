<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> | WhatsGate API</title>
    
    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        primary: {
                            50: '#f5f3ff',
                            100: '#ede9fe',
                            200: '#ddd6fe',
                            300: '#c4b5fd',
                            400: '#a78bfa',
                            500: '#8b5cf6',
                            600: '#7c3aed',
                            700: '#6d28d9',
                            800: '#5b21b6',
                            900: '#4c1d95',
                        }
                    },
                    fontFamily: {
                        'inter': ['Inter', 'sans-serif']
                    }
                }
            }
        }
    </script>
    
    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    
    <!-- FontAwesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    
    <style>
        * {
            font-family: 'Inter', sans-serif;
        }
        
        body {
            background: linear-gradient(135deg, #f8fafc 0%, #f1f5f9 100%);
            min-height: 100vh;
        }
        
        /* Sidebar Styles */
        .sidebar {
            background: linear-gradient(180deg, #1e293b 0%, #0f172a 100%);
            position: fixed;
            left: 0;
            top: 0;
            height: 100vh;
            width: 280px;
            z-index: 100;
            transform: translateX(-100%);
            transition: transform 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            box-shadow: 10px 0 30px rgba(0, 0, 0, 0.2);
        }
        
        @media (min-width: 768px) {
            .sidebar {
                transform: translateX(0);
                position: relative;
                width: 280px;
            }
            
            .main-content {
                margin-left: 280px;
            }
        }
        
        /* Mobile Overlay */
        .mobile-overlay {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: rgba(0, 0, 0, 0.5);
            z-index: 99;
            backdrop-filter: blur(4px);
        }
        
        .mobile-overlay.active {
            display: block;
        }
        
        /* Sidebar Toggle Animation */
        .sidebar.active {
            transform: translateX(0);
        }
        
        /* Nav Link Styles */
        .nav-link {
            transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
            border-radius: 8px;
            position: relative;
            overflow: hidden;
        }
        
        .nav-link:hover {
            background: rgba(255, 255, 255, 0.05);
        }
        
        .nav-link.active {
            background: rgba(139, 92, 246, 0.15);
            border-left: 3px solid #8b5cf6;
        }
        
        .nav-link.active::before {
            content: '';
            position: absolute;
            left: 0;
            top: 0;
            height: 100%;
            width: 3px;
            background: #8b5cf6;
        }
        
        /* Loading Screen */
        #loading {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: white;
            z-index: 9999;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            transition: opacity 0.3s ease;
        }
        
        #loading.hidden {
            opacity: 0;
            pointer-events: none;
        }
        
        .loading-spinner {
            width: 50px;
            height: 50px;
            border: 3px solid #e5e7eb;
            border-top-color: #8b5cf6;
            border-radius: 50%;
            animation: spin 1s linear infinite;
        }
        
        @keyframes spin {
            to { transform: rotate(360deg); }
        }
        
        /* Card Styles */
        .card {
            background: white;
            border-radius: 12px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.05);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            border: 1px solid #e2e8f0;
        }
        
        .card:hover {
            transform: translateY(-4px);
            box-shadow: 0 8px 30px rgba(0, 0, 0, 0.1);
        }
        
        /* Gradient Card */
        .gradient-card {
            background: linear-gradient(135deg, #8b5cf6 0%, #6366f1 100%);
        }
        
        /* Toast Notification */
        .toast {
            position: fixed;
            top: 20px;
            right: 20px;
            z-index: 1000;
            transform: translateX(150%);
            transition: transform 0.3s cubic-bezier(0.68, -0.55, 0.265, 1.55);
        }
        
        .toast.show {
            transform: translateX(0);
        }
        
        /* Mobile Menu Button Animation */
        .mobile-menu-btn {
            display: flex;
            flex-direction: column;
            justify-content: space-between;
            width: 24px;
            height: 18px;
            background: transparent;
            border: none;
            cursor: pointer;
            padding: 0;
        }
        
        .mobile-menu-btn span {
            display: block;
            width: 100%;
            height: 2px;
            background: #4b5563;
            transition: all 0.3s ease;
            border-radius: 1px;
        }
        
        .mobile-menu-btn.active span:nth-child(1) {
            transform: rotate(45deg) translate(6px, 6px);
        }
        
        .mobile-menu-btn.active span:nth-child(2) {
            opacity: 0;
        }
        
        .mobile-menu-btn.active span:nth-child(3) {
            transform: rotate(-45deg) translate(6px, -6px);
        }
        
        /* Responsive Adjustments */
        @media (max-width: 767px) {
            .main-content {
                margin-left: 0 !important;
            }
            
            .mobile-menu-btn {
                display: flex;
            }
            
            .desktop-only {
                display: none !important;
            }
        }
        
        @media (min-width: 768px) {
            .mobile-only {
                display: none !important;
            }
            
            .mobile-menu-btn {
                display: none;
            }
        }
        
        /* Scrollbar Styling */
        ::-webkit-scrollbar {
            width: 6px;
        }
        
        ::-webkit-scrollbar-track {
            background: #f1f1f1;
        }
        
        ::-webkit-scrollbar-thumb {
            background: #c1c1c1;
            border-radius: 3px;
        }
        
        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1a1;
        }
        
        /* Progress Bar */
        .progress-bar {
            height: 8px;
            background: #e5e7eb;
            border-radius: 4px;
            overflow: hidden;
        }
        
        .progress-fill {
            height: 100%;
            background: linear-gradient(90deg, #10b981, #3b82f6);
            border-radius: 4px;
            transition: width 0.5s ease;
        }
        
        /* Button Styles */
        .logout-btn {
            position: relative;
            overflow: hidden;
            transition: all 0.3s ease;
        }
        
        .logout-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(220, 38, 38, 0.3);
        }
        
        .logout-btn:active {
            transform: translateY(0);
        }
    </style>
</head>
<body class="bg-gray-50">
    <!-- Loading Screen -->
    <div id="loading">
        <div class="text-center">
            <div class="loading-spinner"></div>
            <p class="text-gray-700 font-medium text-lg mt-4">Loading Dashboard</p>
            <p class="text-gray-500 text-sm mt-2">Please wait...</p>
        </div>
    </div>
    
    <!-- Mobile Overlay -->
    <div id="mobileOverlay" class="mobile-overlay" onclick="toggleSidebar()"></div>
    
    <!-- Toast Notification -->
    <div id="toast" class="toast">
        <div class="bg-gray-900 text-white px-6 py-4 rounded-xl shadow-lg flex items-center space-x-3 max-w-md">
            <i id="toastIcon" class="fas fa-check-circle text-green-400 text-xl"></i>
            <div class="flex-1">
                <p id="toastMessage" class="font-medium">Operation successful</p>
                <p id="toastDescription" class="text-gray-400 text-sm mt-1"></p>
            </div>
            <button onclick="hideToast()" class="text-gray-400 hover:text-white ml-4">
                <i class="fas fa-times"></i>
            </button>
        </div>
    </div>
    
    <!-- Desktop/Mobile Container -->
    <div class="flex min-h-screen">
        <!-- Sidebar -->
        <div id="sidebar" class="sidebar">
            <!-- Logo -->
            <div class="p-6 border-b border-gray-800">
                <div class="flex items-center space-x-3">
                    <div class="w-10 h-10 rounded-lg bg-gradient-to-br from-purple-600 to-pink-600 flex items-center justify-center flex-shrink-0">
                        <i class="fas fa-comments text-white"></i>
                    </div>
                    <div>
                        <h1 class="text-xl font-bold text-white whitespace-nowrap">WhatsGate</h1>
                        <p class="text-xs text-gray-400 whitespace-nowrap">API Dashboard</p>
                    </div>
                </div>
            </div>
            
            <!-- Navigation -->
            <nav class="flex-1 p-4">
                <div class="space-y-1">
                    <a href="javascript:void(0);" onclick="showSection('overview')" 
                       class="nav-link active flex items-center px-4 py-3 text-gray-200 hover:text-white">
                        <i class="fas fa-chart-line w-5 h-5 flex-shrink-0 mr-3 text-center"></i>
                        <div>
                            <span class="font-medium whitespace-nowrap">Overview</span>
                            <p class="text-xs text-gray-400 mt-1 whitespace-nowrap">Dashboard statistics</p>
                        </div>
                    </a>
                    
                    <a href="javascript:void(0);" onclick="showSection('api')" 
                       class="nav-link flex items-center px-4 py-3 text-gray-200 hover:text-white">
                        <i class="fas fa-code w-5 h-5 flex-shrink-0 mr-3 text-center"></i>
                        <div>
                            <span class="font-medium whitespace-nowrap">API Docs</span>
                            <p class="text-xs text-gray-400 mt-1 whitespace-nowrap">Integration guide</p>
                        </div>
                    </a>
                    
                    <a href="javascript:void(0);" onclick="showSection('info')" 
                       class="nav-link flex items-center px-4 py-3 text-gray-200 hover:text-white">
                        <i class="fas fa-info-circle w-5 h-5 flex-shrink-0 mr-3 text-center"></i>
                        <div>
                            <span class="font-medium whitespace-nowrap">Information</span>
                            <p class="text-xs text-gray-400 mt-1 whitespace-nowrap">Updates & news</p>
                        </div>
                    </a>
                    
                    <a href="javascript:void(0);" onclick="showSection('upgrade')" 
                       class="nav-link flex items-center px-4 py-3 text-gray-200 hover:text-white">
                        <i class="fas fa-rocket w-5 h-5 flex-shrink-0 mr-3 text-center"></i>
                        <div>
                            <span class="font-medium whitespace-nowrap">Upgrade Plan</span>
                            <p class="text-xs text-gray-400 mt-1 whitespace-nowrap">Boost your limits</p>
                        </div>
                    </a>
                    
                    <% if (user.role === 'admin') { %>
                    <a href="/admin" 
                       class="nav-link flex items-center px-4 py-3 text-gray-200 hover:text-white">
                        <i class="fas fa-shield-alt w-5 h-5 flex-shrink-0 mr-3 text-center"></i>
                        <div>
                            <span class="font-medium whitespace-nowrap">Admin Panel</span>
                            <p class="text-xs text-gray-400 mt-1 whitespace-nowrap">System management</p>
                        </div>
                    </a>
                    <% } %>
                </div>
            </nav>
            
            <!-- User Profile -->
            <div class="p-4 border-t border-gray-800">
                <div class="flex items-center mb-4">
                    <div class="w-10 h-10 rounded-full bg-gradient-to-br from-purple-500 to-pink-500 flex items-center justify-center text-white font-bold flex-shrink-0">
                        <%= user.username.charAt(0).toUpperCase() %>
                    </div>
                    <div class="ml-3">
                        <p class="font-semibold text-white truncate"><%= user.username %></p>
                        <div class="flex items-center mt-1">
                            <span class="text-xs px-2 py-1 bg-gradient-to-r from-purple-600/20 to-pink-600/20 text-purple-300 rounded-full">
                                <%= user.plan.toUpperCase() %> PLAN
                            </span>
                        </div>
                    </div>
                </div>
                
                <button id="logoutBtn" 
                        class="logout-btn w-full flex items-center justify-center px-4 py-2.5 bg-gradient-to-r from-red-600 to-red-700 text-white rounded-lg font-medium">
                    <i class="fas fa-sign-out-alt mr-2"></i>
                    <span class="whitespace-nowrap">Logout</span>
                </button>
            </div>
        </div>
        
        <!-- Main Content -->
        <div id="mainContent" class="main-content flex-1 flex flex-col">
            <!-- Header -->
            <header class="bg-white shadow-sm border-b sticky top-0 z-50">
                <div class="px-4 sm:px-6 py-3">
                    <div class="flex items-center justify-between">
                        <!-- Left side: Mobile menu button and title -->
                        <div class="flex items-center space-x-4">
                            <!-- Mobile Menu Button (Hamburger) -->
                            <button id="mobileMenuBtn" class="mobile-menu-btn" onclick="toggleSidebar()">
                                <span></span>
                                <span></span>
                                <span></span>
                            </button>
                            
                            <div>
                                <h1 class="text-lg sm:text-xl font-bold text-gray-900">Dashboard</h1>
                                <p class="text-xs sm:text-sm text-gray-600">Welcome back, <%= user.username %>!</p>
                            </div>
                        </div>
                        
                        <!-- Right side: Actions -->
                        <div class="flex items-center space-x-2 sm:space-x-3">
                            <div class="hidden sm:flex items-center px-4 py-2 bg-gradient-to-r from-primary-500 to-primary-600 text-white rounded-lg font-medium">
                                <i class="fas fa-fire mr-2"></i>
                                <span>Limit: <%= user.requests_used_today %>/<%= user.daily_limit %></span>
                            </div>
                            <a href="https://t.me/yourtelegram" target="_blank" 
                               class="flex items-center px-4 py-2 bg-gradient-to-r from-blue-500 to-blue-600 hover:from-blue-600 hover:to-blue-700 text-white rounded-lg font-medium transition-all duration-200">
                                <i class="fab fa-telegram-plane mr-2"></i>
                                <span class="hidden sm:inline">Support</span>
                            </a>
                        </div>
                    </div>
                </div>
            </header>
            
            <!-- Content Area -->
            <main class="flex-1 p-4 sm:p-6 overflow-y-auto">
                <!-- Overview Section -->
                <section id="overview-section" class="space-y-6">
                    <!-- Stats Grid -->
                    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4 sm:gap-6">
                        <!-- Total Requests All Users -->
                        <div class="card p-5">
                            <div class="flex items-center justify-between mb-4">
                                <div class="p-3 rounded-lg bg-purple-50">
                                    <i class="fas fa-globe text-purple-600"></i>
                                </div>
                                <span class="text-sm font-medium text-gray-500">Global Total</span>
                            </div>
                            <p class="text-2xl font-bold text-gray-900" id="totalRequestsAll">
                                <%= totalRequestsAllUsers.toLocaleString() %>
                            </p>
                            <p class="text-sm text-gray-500 mt-1">All requests across all users</p>
                        </div>
                        
                        <!-- Your Total Requests -->
                        <div class="card p-5">
                            <div class="flex items-center justify-between mb-4">
                                <div class="p-3 rounded-lg bg-blue-50">
                                    <i class="fas fa-chart-bar text-blue-600"></i>
                                </div>
                                <span class="text-sm font-medium text-gray-500">Your Total</span>
                            </div>
                            <p class="text-2xl font-bold text-gray-900">
                                <%= user.total_requests.toLocaleString() %>
                            </p>
                            <p class="text-sm text-gray-500 mt-1">Your lifetime requests</p>
                        </div>
                        
                        <!-- Today's Usage -->
                        <div class="card p-5">
                            <div class="flex items-center justify-between mb-4">
                                <div class="p-3 rounded-lg bg-green-50">
                                    <i class="fas fa-calendar-day text-green-600"></i>
                                </div>
                                <span class="text-sm font-medium text-gray-500">Today's Usage</span>
                            </div>
                            <p class="text-2xl font-bold text-gray-900">
                                <%= user.requests_used_today %>/<%= user.daily_limit %>
                            </p>
                            <div class="mt-4">
                                <div class="flex justify-between text-sm text-gray-600 mb-1">
                                    <span>Usage</span>
                                    <span><%= Math.round((user.requests_used_today / user.daily_limit) * 100) %>%</span>
                                </div>
                                <div class="progress-bar">
                                    <div class="progress-fill" style="width: <%= Math.min((user.requests_used_today / user.daily_limit) * 100, 100) %>%"></div>
                                </div>
                            </div>
                        </div>
                        
                        <!-- Current Plan -->
                        <div class="card p-5">
                            <div class="flex items-center justify-between mb-4">
                                <div class="p-3 rounded-lg bg-yellow-50">
                                    <i class="fas fa-crown text-yellow-600"></i>
                                </div>
                                <span class="text-sm font-medium text-gray-500">Current Plan</span>
                            </div>
                            <div class="flex items-center justify-between">
                                <p class="text-2xl font-bold text-gray-900 uppercase">
                                    <%= user.plan %>
                                </p>
                                <% if (user.plan !== 'free') { %>
                                <span class="px-3 py-1 bg-gradient-to-r from-green-500 to-green-600 text-white text-xs font-bold rounded-full">
                                    ACTIVE
                                </span>
                                <% } %>
                            </div>
                            <% if (user.plan_expires_at) { %>
                            <p class="text-sm text-gray-500 mt-2">
                                <i class="far fa-clock mr-1"></i>
                                Expires: <%= new Date(user.plan_expires_at).toLocaleDateString() %>
                            </p>
                            <% } %>
                        </div>
                    </div>
                    
                    <!-- API Key Card -->
                    <div class="gradient-card rounded-xl p-5 sm:p-6">
                        <div class="flex flex-col sm:flex-row sm:items-center justify-between mb-4 sm:mb-6 gap-4">
                            <div>
                                <h3 class="text-lg sm:text-xl font-bold text-white mb-1">Your API Key</h3>
                                <p class="text-purple-200 text-sm">Keep this secret and never share it publicly</p>
                            </div>
                            <div class="flex space-x-2">
                                <button id="toggleApiKeyBtn" 
                                        class="px-4 py-2 bg-white/10 hover:bg-white/20 text-white rounded-lg transition-all duration-200">
                                    <i id="eyeIcon" class="fas fa-eye mr-2"></i>
                                    <span id="eyeText">Show</span>
                                </button>
                                <button id="copyApiKeyBtn" 
                                        class="px-4 py-2 bg-white hover:bg-gray-100 text-primary-700 rounded-lg font-medium transition-all duration-200">
                                    <i class="fas fa-copy mr-2"></i>
                                    Copy
                                </button>
                            </div>
                        </div>
                        
                        <div class="relative mb-4">
                            <input type="password" 
                                   id="apiKeyInput" 
                                   value="<%= user.api_key %>" 
                                   readonly
                                   class="w-full px-4 py-3 bg-white/10 border border-white/20 rounded-lg text-white font-mono text-sm">
                        </div>
                        
                        <div class="flex items-center text-purple-200 text-sm">
                            <i class="fas fa-shield-alt mr-2"></i>
                            <span>This key grants access to your account. Store it securely.</span>
                        </div>
                    </div>
                    
                    <!-- Recent Activity -->
                    <div class="card overflow-hidden">
                        <div class="p-5 sm:p-6 border-b">
                            <div class="flex flex-col sm:flex-row sm:items-center justify-between gap-4">
                                <h3 class="text-lg sm:text-xl font-bold text-gray-900">Recent Activity</h3>
                                <span class="text-sm text-gray-500">
                                    Last <%= recentLogs.length %> requests
                                </span>
                            </div>
                        </div>
                        
                        <div class="overflow-x-auto">
                            <table class="w-full">
                                <thead>
                                    <tr class="bg-gray-50">
                                        <th class="py-3 px-4 text-left text-sm font-medium text-gray-700">Endpoint</th>
                                        <th class="py-3 px-4 text-left text-sm font-medium text-gray-700">Method</th>
                                        <th class="py-3 px-4 text-left text-sm font-medium text-gray-700">Time</th>
                                        <th class="py-3 px-4 text-left text-sm font-medium text-gray-700">Status</th>
                                    </tr>
                                </thead>
                                <tbody class="divide-y divide-gray-200">
                                    <% if (recentLogs.length === 0) { %>
                                    <tr>
                                        <td colspan="4" class="py-8 px-4 text-center">
                                            <div class="flex flex-col items-center justify-center">
                                                <div class="w-12 h-12 rounded-full bg-gray-100 flex items-center justify-center mb-3">
                                                    <i class="fas fa-inbox text-gray-400"></i>
                                                </div>
                                                <p class="text-gray-500 font-medium">No activity yet</p>
                                                <p class="text-gray-400 text-sm mt-1">Your API requests will appear here</p>
                                            </div>
                                        </td>
                                    </tr>
                                    <% } else { %>
                                        <% recentLogs.forEach(log => { %>
                                        <tr class="hover:bg-gray-50">
                                            <td class="py-3 px-4 font-mono text-sm">
                                                <%= log.endpoint %>
                                            </td>
                                            <td class="py-3 px-4">
                                                <span class="px-2 py-1 rounded text-xs font-medium 
                                                      <%= log.method === 'GET' ? 'bg-blue-100 text-blue-800' : 
                                                         log.method === 'POST' ? 'bg-green-100 text-green-800' : 
                                                         'bg-gray-100 text-gray-800' %>">
                                                    <%= log.method %>
                                                </span>
                                            </td>
                                            <td class="py-3 px-4 text-gray-600 text-sm">
                                                <%= new Date(log.created_at).toLocaleTimeString([], {hour: '2-digit', minute:'2-digit'}) %>
                                            </td>
                                            <td class="py-3 px-4">
                                                <span class="px-2 py-1 rounded text-xs font-medium bg-green-100 text-green-800">
                                                    <i class="fas fa-check mr-1"></i>Success
                                                </span>
                                            </td>
                                        </tr>
                                        <% }); %>
                                    <% } %>
                                </tbody>
                            </table>
                        </div>
                    </div>
                </section>
                
                <!-- Other sections remain the same -->
                <section id="api-section" class="space-y-6 hidden">
                    <div class="card p-5 sm:p-6">
                        <h2 class="text-xl font-bold text-gray-900 mb-4">API Documentation</h2>
                        <!-- API docs content -->
                    </div>
                </section>
                
                <section id="info-section" class="space-y-6 hidden">
                    <div class="card p-5 sm:p-6">
                        <h2 class="text-xl font-bold text-gray-900 mb-4">Information</h2>
                        <!-- Info content -->
                    </div>
                </section>
                
                <section id="upgrade-section" class="space-y-6 hidden">
                    <div class="card p-5 sm:p-6">
                        <h2 class="text-xl font-bold text-gray-900 mb-4">Upgrade Plan</h2>
                        <!-- Upgrade content -->
                    </div>
                </section>
            </main>
        </div>
    </div>
    
    <script>
        // ==================== INITIALIZATION ====================
        
        // Hide loading when DOM is ready
        document.addEventListener('DOMContentLoaded', function() {
            console.log('DOM Content Loaded');
            
            // Hide loading after short delay
            setTimeout(function() {
                const loading = document.getElementById('loading');
                if (loading) {
                    loading.classList.add('hidden');
                    setTimeout(function() {
                        loading.style.display = 'none';
                        console.log('Loading screen hidden');
                    }, 300);
                }
            }, 500);
            
            // Initialize dashboard functionality
            initializeDashboard();
        });
        
        function initializeDashboard() {
            console.log('Initializing dashboard...');
            
            try {
                // Setup all event listeners
                setupEventListeners();
                
                // Show initial section
                const hash = window.location.hash.substring(1) || 'overview';
                showSection(hash);
                
                console.log('Dashboard initialized successfully');
                
            } catch (error) {
                console.error('Error initializing dashboard:', error);
                showToast('Error initializing dashboard', 'error');
            }
        }
        
        // ==================== EVENT LISTENERS ====================
        
        function setupEventListeners() {
            console.log('Setting up event listeners...');
            
            // Mobile menu button
            const mobileMenuBtn = document.getElementById('mobileMenuBtn');
            if (mobileMenuBtn) {
                mobileMenuBtn.addEventListener('click', toggleSidebar);
                console.log('Mobile menu button listener added');
            }
            
            // Mobile overlay
            const mobileOverlay = document.getElementById('mobileOverlay');
            if (mobileOverlay) {
                mobileOverlay.addEventListener('click', toggleSidebar);
                console.log('Mobile overlay listener added');
            }
            
            // Logout button - FIXED HERE
            const logoutBtn = document.getElementById('logoutBtn');
            if (logoutBtn) {
                console.log('Logout button found, adding event listener');
                logoutBtn.addEventListener('click', handleLogout);
                console.log('Logout button listener added');
            } else {
                console.error('Logout button not found!');
            }
            
            // API Key toggle button
            const toggleApiKeyBtn = document.getElementById('toggleApiKeyBtn');
            if (toggleApiKeyBtn) {
                toggleApiKeyBtn.addEventListener('click', function() {
                    const input = document.getElementById('apiKeyInput');
                    const eyeIcon = document.getElementById('eyeIcon');
                    const eyeText = document.getElementById('eyeText');
                    
                    if (input.type === 'password') {
                        input.type = 'text';
                        eyeIcon.className = 'fas fa-eye-slash mr-2';
                        eyeText.textContent = 'Hide';
                    } else {
                        input.type = 'password';
                        eyeIcon.className = 'fas fa-eye mr-2';
                        eyeText.textContent = 'Show';
                    }
                });
                console.log('API Key toggle button listener added');
            }
            
            // API Key copy button
            const copyApiKeyBtn = document.getElementById('copyApiKeyBtn');
            if (copyApiKeyBtn) {
                copyApiKeyBtn.addEventListener('click', function() {
                    const input = document.getElementById('apiKeyInput');
                    input.select();
                    input.setSelectionRange(0, 99999);
                    
                    try {
                        navigator.clipboard.writeText(input.value);
                        showToast('API Key copied to clipboard!', 'success');
                    } catch (err) {
                        console.error('Copy failed:', err);
                        showToast('Failed to copy', 'error');
                    }
                });
                console.log('API Key copy button listener added');
            }
            
            // Navigation links
            const navLinks = document.querySelectorAll('.nav-link');
            navLinks.forEach(link => {
                link.addEventListener('click', function() {
                    // Remove active class from all links
                    navLinks.forEach(l => l.classList.remove('active'));
                    // Add active class to clicked link
                    this.classList.add('active');
                });
            });
            console.log('Navigation links listeners added');
            
            // Window resize handler
            window.addEventListener('resize', handleResize);
            console.log('Window resize listener added');
        }
        
        // ==================== SIDEBAR FUNCTIONS ====================
        
        function toggleSidebar() {
            console.log('Toggling sidebar');
            
            try {
                const sidebar = document.getElementById('sidebar');
                const overlay = document.getElementById('mobileOverlay');
                const menuBtn = document.getElementById('mobileMenuBtn');
                
                if (sidebar && overlay && menuBtn) {
                    sidebar.classList.toggle('active');
                    overlay.classList.toggle('active');
                    menuBtn.classList.toggle('active');
                    
                    // Prevent body scroll when sidebar is open on mobile
                    if (sidebar.classList.contains('active')) {
                        document.body.style.overflow = 'hidden';
                    } else {
                        document.body.style.overflow = '';
                    }
                    
                    console.log('Sidebar toggled successfully');
                } else {
                    console.error('Sidebar elements not found');
                }
            } catch (error) {
                console.error('Error in toggleSidebar:', error);
            }
        }
        
        function closeSidebar() {
            try {
                const sidebar = document.getElementById('sidebar');
                const overlay = document.getElementById('mobileOverlay');
                const menuBtn = document.getElementById('mobileMenuBtn');
                
                if (sidebar && overlay && menuBtn) {
                    sidebar.classList.remove('active');
                    overlay.classList.remove('active');
                    menuBtn.classList.remove('active');
                    document.body.style.overflow = '';
                }
            } catch (error) {
                console.error('Error in closeSidebar:', error);
            }
        }
        
        // ==================== SECTION MANAGEMENT ====================
        
        function showSection(section) {
            console.log('Showing section:', section);
            
            try {
                // Hide all sections
                document.querySelectorAll('main > section').forEach(sec => {
                    sec.classList.add('hidden');
                });
                
                // Show selected section
                const targetSection = document.getElementById(section + '-section');
                if (targetSection) {
                    targetSection.classList.remove('hidden');
                    
                    // Update URL hash
                    history.replaceState(null, null, '#' + section);
                    
                    // Close sidebar on mobile
                    if (window.innerWidth < 768) {
                        closeSidebar();
                    }
                    
                    console.log('Section shown successfully');
                } else {
                    console.error('Target section not found:', section);
                }
            } catch (error) {
                console.error('Error in showSection:', error);
            }
        }
        
        // ==================== LOGOUT FUNCTION ====================
        
        async function handleLogout() {
            console.log('Logout button clicked!');
            
            // First, show confirmation dialog
            if (!confirm('Are you sure you want to logout?')) {
                console.log('Logout cancelled by user');
                return;
            }
            
            console.log('Proceeding with logout...');
            
            try {
                // Show loading screen
                const loading = document.getElementById('loading');
                if (loading) {
                    loading.style.display = 'flex';
                    loading.style.opacity = '1';
                    loading.classList.remove('hidden');
                }
                
                console.log('Sending logout request to /auth/logout...');
                
                // Send logout request
                const response = await fetch('/auth/logout', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                        'Accept': 'application/json'
                    },
                    credentials: 'include' // Important for cookies/session
                });
                
                console.log('Logout response status:', response.status);
                
                // Try to parse response as JSON
                let data;
                try {
                    data = await response.json();
                    console.log('Logout response data:', data);
                } catch (jsonError) {
                    console.log('Response is not JSON, treating as plain text');
                    data = { success: response.ok };
                }
                
                if (data.success || response.ok) {
                    console.log('Logout successful, redirecting...');
                    
                    // Clear any local storage
                    localStorage.clear();
                    sessionStorage.clear();
                    
                    // Redirect to login page
                    const redirectUrl = data.redirect || '/login';
                    console.log('Redirecting to:', redirectUrl);
                    window.location.href = redirectUrl;
                    
                } else {
                    throw new Error(data.error || 'Logout failed with status: ' + response.status);
                }
                
            } catch (error) {
                console.error('Logout error:', error);
                
                // Show error toast
                showToast('Logout failed: ' + error.message, 'error');
                
                // Fallback: try direct redirect after 2 seconds
                setTimeout(() => {
                    console.log('Fallback redirect to /login');
                    window.location.href = '/login';
                }, 2000);
                
            } finally {
                // Hide loading screen
                const loading = document.getElementById('loading');
                if (loading) {
                    loading.style.display = 'none';
                }
            }
        }
        
        // ==================== TOAST NOTIFICATION ====================
        
        function showToast(message, type) {
            try {
                const toast = document.getElementById('toast');
                const toastIcon = document.getElementById('toastIcon');
                const toastMessage = document.getElementById('toastMessage');
                
                if (toast && toastIcon && toastMessage) {
                    // Set message
                    toastMessage.textContent = message;
                    
                    // Set icon based on type
                    switch(type) {
                        case 'success':
                            toastIcon.className = 'fas fa-check-circle text-green-400 text-xl';
                            break;
                        case 'error':
                            toastIcon.className = 'fas fa-exclamation-circle text-red-400 text-xl';
                            break;
                        case 'warning':
                            toastIcon.className = 'fas fa-exclamation-triangle text-yellow-400 text-xl';
                            break;
                        default:
                            toastIcon.className = 'fas fa-info-circle text-blue-400 text-xl';
                    }
                    
                    // Show toast
                    toast.classList.add('show');
                    
                    // Hide after 3 seconds
                    setTimeout(() => {
                        toast.classList.remove('show');
                    }, 3000);
                }
            } catch (error) {
                console.error('Error in showToast:', error);
            }
        }
        
        function hideToast() {
            const toast = document.getElementById('toast');
            if (toast) {
                toast.classList.remove('show');
            }
        }
        
        // ==================== UTILITY FUNCTIONS ====================
        
        function handleResize() {
            // Close sidebar on mobile when resizing to desktop
            if (window.innerWidth >= 768) {
                closeSidebar();
            }
        }
        
        // ==================== MAKE FUNCTIONS GLOBALLY AVAILABLE ====================
        
        // Export functions to global scope for inline onclick handlers
        window.toggleSidebar = toggleSidebar;
        window.showSection = showSection;
        window.handleLogout = handleLogout;
        window.showToast = showToast;
        window.hideToast = hideToast;
        
        console.log('Dashboard functions ready and exported');
    </script>
</body>
</html>
